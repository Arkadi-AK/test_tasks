version: "3"

services:

  wb_app:
    restart: always
    build:
      context: ./wb_app
    ports:
      - "8000:8000"
    command: ["python3", "manage.py", "runserver", "0.0.0.0:8000"]
    depends_on:
      - redis
    volumes:
      - ./:/app
